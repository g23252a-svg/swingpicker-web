# --- pip options (Safe for Streamlit Cloud & CI/CD) ---
# CPU 전용 환경에서 torch 설치 실패를 방지하는 최상단 선언
--extra-index-url https://download.pytorch.org/whl/cpu

# --- [Bootstrap: 반드시 최상단] ---
setuptools                # 👈 pykrx 가 pkg_resources 에 의존 → 선설치 필수

# --- [Core UI] ---
streamlit>=1.34.0
plotly>=5.20.0

# --- [Data Science & ML] ---
pandas>=2.2.0
numpy>=1.26.0
# [v56] parquet 엔진 — 어느 requirements에도 선언돼 있지 않았다.
#   alpha_engine / pipeline_data / services.pick_reliability / components.tab_stocks 등
#   10개 모듈이 ohlcv_cache_*.parquet를 읽고 쓴다. pandas 3.x는 pyarrow를 필수
#   의존성으로 끌어오지만 pandas 2.x는 아니다 — 즉 pandas 2.x로 해석되는 환경에서는
#   생존편향 제거(v50)용 패널 합집합이 폴백/실패한다.
#   v56에서 CI가 전체 스위트를 돌리기 시작하자 29건 실패로 드러났다(원인 1개).
pyarrow>=15.0.0
scikit-learn>=1.4.0
xgboost>=2.0.0
torch>=2.2.0              # 👈 CPU-only (~200MB via extra-index-url)

# --- [Database & Async] ---
duckdb>=0.10.0
aiohttp>=3.9.0
requests>=2.31.0
tqdm>=4.66.0

# --- [Finance Data & DART] ---
finance-datareader>=0.9.6
pykrx==1.0.51             # 👈 [검증됨] 안정적인 Harbor 버전
opendartreader            # 👈 설치 호환성 중심의 유연한 선언
yfinance>=0.2.36          # 👈 [v20.0] 벤치마크 yfinance 폴백 (해외 IP에서도 동작)

# --- [Web Scraping & AI] ---
google-genai>=1.0.0       # 👈 신규 통합 SDK (구 google-generativeai EOL 2025-11-30)
beautifulsoup4>=4.12.0
lxml>=5.2.0               # 👈 크롤러 파싱 속도/정확도 핵심

# --- [Infrastructure & Utils] ---
python-dotenv>=1.0.1      # 👈 환경 변수(API KEY) 보안 게이트
pytz>=2024.1              # 👈 KST 시간 동기화

# --- [제거된 패키지 (미사용 확인 완료)] ---
# shap                    ❌ 코드베이스 어디에서도 import 없음 (~400MB 절감)
# joblib                  ❌ 직접 import 없음 (scikit-learn 내부 의존으로 자동 설치됨)
# extra-streamlit-components  ❌ import/사용처 없음
# xmltodict               ❌ import/사용처 없음
# google-generativeai     ❌ 2025-11-30 EOL → google-genai 로 교체
