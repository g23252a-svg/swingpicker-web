name: Auto Collect and Push CSV

permissions:
  contents: write

on:
  schedule:
    - cron: "0 14 * * *"   # UTC 14:00 = KST 23:00
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          python -V
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python - <<'PY'
import pykrx, sys
print("pykrx version:", pykrx.__version__)
PY

      - name: Run Collector
        run: python collector.py

      - name: List outputs
        run: |
          ls -alh data || true
          head -n 5 data/recommend_latest.csv || true
          head -n 5 data/krx_codes.csv || true

      - name: Commit & Push CSV
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # data/*.csv 이 없으면 add를 건너뛰도록
          shopt -s nullglob
          files=(data/*.csv)
          if (( ${#files[@]} )); then
            git add "${files[@]}"
            git status -s
            git commit -m "chore: add CSV $(date -u +'%Y-%m-%d %H:%M:%S UTC')" || echo "no changes"
            git push || echo "no push"
          else
            echo "No CSV files to commit."
          fi
